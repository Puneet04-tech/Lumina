[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "18.17.0"
  NPM_VERSION = "9.6.7"
  NEXT_PUBLIC_API_URL = "https://lumina-7mir.onrender.com"

# Redirects are handled via _redirects file in public directory

